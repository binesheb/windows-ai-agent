server:
  host: 127.0.0.1
  port: 8765

security:
  mode: read_only
  remote_connections: false

filesystem:
  workspaces:
    - name: agent
      path: .
      access: read
  max_read_bytes: 1048576
  max_entries: 500

logging:
  audit_file: logs/audit.jsonl
